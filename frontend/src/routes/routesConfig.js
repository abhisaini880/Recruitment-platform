const routesConfig = {
  // Public Routes
  home: "/",
  auth: "/auth",

  // Protected Routes
  app: "/app",
  dashboard: "/app/dashboard",
  settings: "/app/settings",
  jobboard: "/app/jobboard",

  // Not Found
  pageNotFound: "*",
}

export default routesConfig
